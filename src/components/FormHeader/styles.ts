import { defaultBlack, SegoeUIBoldItalic } from "@shared/themes";
import styled from "styled-components/native";

export const FormTypeText = styled.Text`
  font-size: 24px;
  color: ${defaultBlack};
  font-family: ${SegoeUIBoldItalic};
  margin-top: 40px;
`;
